"use strict";

wetterApp.controller('WetterOnlineCtrl', function($scope, $resource, $cookies, $locale, WetterOnlineService) {

  $scope.isAdmin = false;

  console.log('+++ Locale: ' + $locale.id);

  WetterOnlineService.berlin_pankow().$promise
  .then(function(data){
	  console.log('Daten wurden geladen');
	  $scope.wetter = data;
	  console.log(data);
  });
  

});
